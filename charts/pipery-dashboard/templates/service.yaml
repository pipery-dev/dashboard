apiVersion: v1
kind: Service
metadata:
  name: {{ include "pipery-dashboard.fullname" . }}
  labels:
    {{- include "pipery-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pipery-dashboard.selectorLabels" . | nindent 4 }}
